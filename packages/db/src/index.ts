/**
 * Data access package (Phase 0 placeholder).
 *
 * From Phase 1 onwards it will contain:
 * - Types generated from the Supabase schema.
 * - Client factory helpers (browser, server, worker with service role).
 * - Typed, RLS-aware queries.
 *
 * Non-negotiable rule: the service role key is never exposed to the client.
 */
export const DB_PACKAGE_READY = false;
