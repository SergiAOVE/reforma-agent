# RLS policies

RLS policies must be applied in order with the schema, so they are versioned as a regular
migration: see `../migrations/20260702120200_enable_rls.sql` (fully commented).

This folder is kept for future standalone policy documents (e.g. Storage bucket policies in
Phase 4).
